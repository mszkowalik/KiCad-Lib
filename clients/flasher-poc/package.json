{
  "name": "flasher-poc",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "test": "esbuild test_sim.mjs --bundle --platform=node --format=esm --outfile=.test.bundle.mjs --log-level=error && node .test.bundle.mjs"
  },
  "dependencies": {
    "esptool-js": "^0.6.0",
    "js-md5": "^0.9.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "vite": "^8.1.5"
  }
}