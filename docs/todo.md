# Work list

What the platform still needs: built, fixed or decided. One row per item, with
the reason it matters and what proves it done.

**This file holds open work only.** Ask before you add a row, unless the user
asked for that item first. Delete a row when it is done — record the result in
its proper home: a [decision record](decisions/index.md), a
`.claude/skills/` update via `propose_skill_update`, or
[CHANGELOG.md](../CHANGELOG.md). Git and the changelog hold what a deleted row
said. Never strike a row through and leave it.

**This file holds work, not facts.** A choice still to make goes to a proposed
[decision record](decisions/index.md) instead. A row here can point at one.

## Open

| # | Item | Why | Done when |
|---|---|---|---|
| 23 | `BLM18EG101TN1D` carries `Value = 68nH` where the ferrite-bead rule says impedance | conventions-library says a ferrite bead's `Value` is its impedance at the test frequency, and this part's `Impedance` property reads `100Ω@100MHz` while its `Value` reads `68nH`. Both are real published specs for the part, which is why it was left for a decision rather than changed — and why no check covers it: the Inductors `cmp.value_field` has no ferrite variant. Found in the 2026-07-25 Value audit and carried in the skill until the 2026-09-14 thinning moved it here | The user picks which spec the Value carries, the part matches it, and a ferrite variant of `cmp.value_field` enforces it |
| 24 | `Inductors`, `RF` and `Circuit_Protection` carry no `comp_type` | Their Value and description rules cannot become checks without one: a fixed inductor, a ferrite bead and a LAN transformer are three different rules sharing one category, with nothing in the data to tell them apart. Diodes shows the shape that works — `comp_type` of TVS / ZENER / DIODE, and `cmp.value_field` splits on it. `Connectors` has the same problem differently: `Pluggable terminal block; {Pitch}` and `Pluggable terminal block; 3.5mm` coexist in one family, so there is no single correct value to compare against | Each of the four categories carries a discriminator, and its Value rule is a variant group rather than a paragraph |
| 25 | Decide the symbol pin grid: 2.54 mm or 1.27 mm | `conventions-symbols` states 2.54 mm in both axes as absolute and claims an off-grid pin "cannot be wired on a default sheet". Measured 2026-09-14: `sym.pins_grid` enforces **1.27 mm** and no symbol violates it, while **61 of 206** are off 2.54 — `RPi_CM5` (200 pins), `ZED-F9P` (102), every large STM32. Dense parts do not fit on 2.54 mm, and KiCad's default schematic grid includes 50 mil, so the "cannot be wired" claim is not so either. The skill now records both numbers and says the choice is open | One number is the house rule, `sym.pins_grid` carries it, and the skill states it once |
| 22 | Three property families the conventions skill listed as follow-ups | Moved out of `conventions-library` on 2026-09-14, where a work register did not belong. (a) The ~36-member Connectors terminal-block family uses three free-text description conventions and wants one `{Pitch}`-based template; both Connectors chunks have to be reviewed together. (b) Buttons need verified Length/Width/Height/Force properties backfilled across the category before they can be templated. (c) BJTs carry `Continuous Drain Current` for collector current, a leftover from the MOSFET base template — the values are right, the key is wrong, and it moves as one pass across every sibling or not at all | Each family has one template in its category's `cmp.description` check, and the BJT key is renamed everywhere at once |
| 1 | Draw mechanical footprints for the three Italtronic enclosures — `Italtronic_05.0502530`, `Italtronic_35.0207000.BL`, `Italtronic_P05050201P.BL` | Every other `Mechanical_7S` enclosure has its own land (`Enclosure_Hammond_1551RFLGY`, `_1551TFLGY`, `_1551XFLGY`, `_1556CGY`, `Enclosure_TAKACHI_SIM6-12-3W`); these three are the only members of the family without one, so their outline and keepout are invisible on a layout. Raised by a review pass on 2026-08-25 and still flagged on all three parts. [Decision 0005](decisions/0005-off-board-parts.md) stopped the machine tier failing them but deliberately did not settle this — assigning a footprint puts each part back on the board automatically, so the two are compatible in either order | A footprint exists for each of the three, is assigned, and the `custom:footprint-missing-enclosure` flag on each part is answered rather than left open |
| 2 | Run the datasheet clean-up on production: deploy, let the startup migration move the bytes, then `POST /api/datasheets/restamps/collapse` | The code is committed but production still carries the old schema and the history the byte rule wrote — 704 versions over 557 distinct files, 1079 MB in `datasheet_versions`, 1981 MB of database. The local rehearsal on a copy of that data gave 616 versions over 484 files and 602 MB, and the server has 75 GB free, so the table rewrite has room. The migration is idempotent and the collapse refuses to run while the classification backfill is still going ([decision 0004](decisions/0004-datasheet-identity-and-storage.md)) | `GET /api/datasheets/restamps` on production returns nothing, `GET /api/datasheets/storage` reports fewer documents than versions, and the referential check finds no dangling pin, current version or orphan |
| 3 | Watch a manufacturer's product page, not only its PDF: register a page URL per component, re-open it on a schedule, and report new documents and lifecycle changes as findings | The original request behind [decision 0004](decisions/0004-datasheet-identity-and-storage.md), which built only the identity it needs. Measured 2026-09-10 over 20 vendor sites: a plain HTTP scrape finds the datasheet link on 3 of 17 pages, headless Chromium raises that to about 7, and Microchip, Analog Devices and ST refuse both from this network — so resolving a page must be a one-off step with a human or the agent confirming the document, while the nightly job stays a conditional GET on the resolved PDF. Chromium is in no image today. The orderable-part and lifecycle table the identity hash already extracts from a TI datasheet is the same signal, arriving for free | A component can carry a watched page URL, a scheduled pass reports a new document or a status change as a review finding rather than attaching it automatically, and a page the fetcher cannot reach is reported as unreachable instead of unchanged |

### Verification overhaul (agreed 2026-09-14)

Sequenced. Each phase is usable on its own, and each unblocks the next — the
order is not preference, it is dependency. **Rows 4, 5 and 6 are done**
(severity, standing exceptions, computed conformance) and were deleted from this
table as they landed; they live in decisions
[0016](decisions/0016-severity-and-standing-exceptions.md) and
[0017](decisions/0017-conformance-is-computed-not-recorded.md). Decisions
[0013](decisions/0013-the-validator-owns-the-automatic-checks.md),
[0014](decisions/0014-a-check-carries-its-own-configuration.md) and
[0015](decisions/0015-a-check-says-which-subjects-it-is-about.md) hold the
reasoning for what is already built; each phase below gets its own record
before it lands. **Every phase includes its UI — a feature the editor or the
review card cannot reach is not done.**

| # | Item | Why | Done when |
|---|---|---|---|
| 7 | **Finish moving the convention skills into checks** | Eight checks landed on 2026-09-14 and the Diodes Value variants followed (CHANGELOG). Two rules are left and each is blocked on something the platform has not got. The **manufacturer `one_of`**: the canonical table covers 360 of 427 filled-in manufacturers, and the other 67 are mostly names the TABLE is missing (`Nexperia` x10, `Littelfuse` x6, `Molex`, `Kingbright`, `Raspberry Pi`, `Quectel`) rather than data that is wrong — with two genuine duplicates of listed names, `Murata Electronics` against `Murata` and `Infineon` against `Infineon Technologies`. **`fp.pad_grid`**: 74 of 139 coarse-pitch footprints sit off the 0.1 mm grid and almost all are KiCad stock copper the tier rule freezes, but the platform records no provenance, so "house-authored" is not decidable | The manufacturer table is complete and the `one_of` ships; a footprint records whether its copper is house-authored or stock, and `fp.pad_grid` ships against the house ones |
| 9 | **`disallow`, and a table-shaped rule** | Two more KiCad borrows. Several rules are "there must be no X" (nothing electrical on the top edge, no zero-annulus plated pad, no `no_connect` on a stacked pin) and read backwards as `at_most 0`. And KiCad's ERC pin map is a 12x12 MATRIX, not a rule list — a reminder that not every rule fits the rule-list shape | `disallow` is an assertion with UI; a decision record settles whether the pin-type rules want a table |
| 10 | **Claims with their own dependency, replacing the carry** | A record is versioned against the whole version, so a silkscreen edit can cost a pinout verification and 675 carry records exist to stop it. A claim should name what it depends on — the pin fingerprint, the datasheet identity, the property set — and be live while those hold. `services/material.py` already computes them, as one all-or-nothing gate | A claim carries `depends_on`; liveness is derived, not carried; `carry_geometry` and `carry_component` are gone |
| 11 | **Finish splitting the state into three facts** | The review card and the queue landed on 2026-09-14: a card reads `ISSUES · FAILS · JUDGED 10/11` and a queue row carries the same in its tooltip, because a DataTable row is one line tall and must not wrap. What is left is the surfaces that still read one word — the PROJECT REVIEW and the production RUN GATE, which decide whether a board may be built on a state that cannot tell "nobody looked" from "one item is open" | `conforms` / `judged n of m` / `signed` are separate on the project review and the run gate too |
| 14 | Retire `cmp.base_symbol_allowed` | Subsumed by `{fact: "$base_symbol", one_of: [...]}` once phase 8 lands. Two mechanisms for one rule is the thing decisions 0013-0015 exist to prevent | The key is out of `_CHECK_SPECS` and a migration has dropped the item from every stored list — a half-applied one leaves an item claiming `machine: true` for an unknown key, which the save path refuses |
| 16 | Narrow `custom:` keys once phase 7 lands | 188 distinct keys, 301 answers, 150 used exactly once, every one written by an agent. Those are 150 unfiled requests for a checklist item, recorded privately instead | An agent proposing a repeated custom key is steered to propose a checklist item; the one-off case still works |
| 17 | Decide `comp_type` versus subcategories | `comp_type` is read by NOTHING in the platform, is a required property on Diodes, and is emitted to KiCad on 26 symbols in `Diodes.kicad_sym`. It is a subcategory wearing a property's clothes. `Category.parent_id` and the POST/PATCH/DELETE endpoints have always existed; the tree is flat and there is no screen. Costs ~41 published versions and their verifications | A decision record settles it; if subcategories win, the screen exists, the parts have moved, `comp_type` is retired and the variants keyed on it are deleted |
| 20 | Retire the checks the new ones supersede | The prefill landed on 2026-09-14 (CHANGELOG): eight checks live on production, measured, with the six documented Value deviations recorded as standing exceptions. What is left is the other half of the row — a check the new rules make redundant is still asked, so the library carries two rules for one thing. `cmp.base_symbol_allowed` is the known case (row 14); the rest wants one pass over the 25 component, 11 symbol and 20 footprint checks now that `assert` exists | Every check the new ones subsume is gone, and the removal is recorded |
| 21 | Repoint or retire the 188 agent-invented `custom:` keys | 301 answers, 150 keys used exactly once, every one written by an agent. Each is either a check that should exist (repoint it) or a genuine one-off. Left alone they are 188 private vocabularies competing with the real checklist | Every custom key is either mapped onto a real check or deliberately kept; the mapping is recorded; `custom:` is narrowed per row 16 |

## More information

* [decisions/index.md](decisions/index.md) — decisions still `proposed`.
* [CLAUDE.md](../CLAUDE.md) — how a finding routes to a decision, a skill, a
  checklist item or this file.
