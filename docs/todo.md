# Work list

What the platform still needs: built, fixed or decided. One row per item, with
the reason it matters and what proves it done.

**This file holds open work only.** Ask before you add a row, unless the user
asked for that item first. Delete a row when it is done — record the result in
its proper home: a [decision record](decisions/index.md), a
`.claude/skills/` update via `propose_skill_update`, or
[CHANGELOG.md](../CHANGELOG.md). Git and the changelog hold what a deleted row
said. Never strike a row through and leave it.

**This file holds work, not facts.** A choice still to make goes to a proposed
[decision record](decisions/index.md) instead. A row here can point at one.

## Open

| # | Item | Why | Done when |
|---|---|---|---|
| 30 | **The BOM plan does not know about substitutions** | [0038](decisions/0038-a-substitution-belongs-to-the-batch.md) records what a batch FITTED, and the Stock page raises a standing finding while the design still names the superseded part. Nothing else reads it: `project_bom`, the planned side of the Materials table and every purchasing forecast still take the design's part, so they keep planning and pricing a component the last two batches did not use. That is the exact failure the record was written about — on 2026-08-06, 476 pieces of `C110548` were bought at $1.2296 against a historic $0.336, three months after the position moved to `C7223`, because purchasing read the design. The banner is a sign beside the hole, not the fix; the real fix is the schematic, which is what `design_updated` claims. The open question is whether a substitution should ALSO steer the plan while the design lags, and that is a decision, not an obvious yes: a plan that quietly follows a batch-scoped fact stops matching the drawing | A decision record settles whether a live substitution steers the plan, and whatever it says is implemented and visible on the planned side |
| 31 | **Nothing detects a substitution on a board the supplier does not BOM** | Detection compares JLCPCB's cached BOM for a batch against its own previous one, so it only sees JLC-assembled orders. A batch assembled by LIFTECH has no supplier BOM at all: `substitutions.unused` correctly returns `has_supplier_bom: false` and reports nothing, and no candidate is ever proposed. Hand entry covers it — the Materials row has *Substitute this part* on every batch — but nobody is prompted, so the gap is silent rather than wrong. It matters because a subcontractor substituting a part is at least as likely as JLC doing it, and the platform would learn about it only from someone noticing the stock did not move | Either an assembler's own BOM or build report can be attached to a batch and read by the same detector, or a decision records that substitutions on non-JLC batches are entered by hand and the Materials tab says so where the absence is visible |
| 32 | **31 positions across seven batches show `not on the board`, and none is recorded** | Since 2026-09-19 a planned position absent from both JLCPCB's BOM for the batch and every draw is flagged on its Materials row. Twenty-four of the 31 are on *Run #0 — prototypes V3.1*, whose snapshot (id 1, 2026-07-19) diverges sharply from what JLC actually built; the rest are `C398053` TS3625A at BOOT1,RESET1 on dongle Batches 2 and 3, `C15195` 10nF at C26-C31 on Aqua Batches 2 to 5, and `C25748` 118K at R7 on *Run #2 — prototypes V3.3*, which is also an unrecorded substitution candidate (`matchType: update`, superseded part still in the design). Each is one click — a substitution, or quantity 0 for "left empty" — but each is a claim about what physically happened, so none was made for the user. Left unanswered the flag becomes decoration, which is how a warning stops being read | Every flagged position carries either a substitution or a recorded reason, and any that turn out to be a defect in the check are fixed rather than dismissed |
| 33 | **500 pieces of `C110548` sit at JLCPCB with no consumer** | `T491D107K016AT`, the tantalum the CE_Dongle_V2 schematic still names at C2. The position moved to `C7223` at Batch 7 (2026-05) and the design was never updated, so on 2026-08-06 a further 476 were bought for $585.29 — **$1.2296 each against a historic $0.225-$0.336** — and JLC's ledger confirms all 500 arrived and nothing has drawn them since Batch 6. They are real stock and the pool values them at the price paid, so the batch that eventually consumes them inherits a unit cost five times the usual one. This is a choice, not a defect: use them on a batch, sell them back, or write them off against the decision that replaced the part | The 500 are consumed, disposed of, or written off with the reason recorded, and `parts-stock` no longer shows them as unconsumed remainder |
| 34 | **The carton supply before 2024-11-28 has no invoice, only a placeholder** | `PLACEHOLDER-KARTON-2024` (doc 3187, 2024-09-29) books 800 Dongle and 1000 Aqua cartons that no document evidences. The stock demonstrably existed: LifTech's count of 2025-06-08 reports **1250 Aqua cartons** on the shelf when only 1000 had ever been invoiced and 250 were drawn, and a shelf cannot hold more than was bought — so the Aqua gap is immune to any re-timing of draws. Searched 2026-09-21 without result: all 1136 PDFs under `~/Documents/7Sigma` and `~/Documents/9Sigma`, Gmail from 2024-10 to 2025-09, and 9Sigma ledger account 202-13, which totals 1 143,90 PLN for PUDLO — FA 1483/24 alone. **One alternative was tested and rejected**: if every batch had shipped one delivery-cycle later than its run date, the four real invoices would cover all 4600 Dongle cartons with zero bare devices and hit the counted 200 exactly, needing no Dongle placeholder — but it requires Batch 4 to have shipped ~6 months after it was made, and the operator confirms Batch 4 shipped on time (2026-09-21). **The 200-carton Dongle gap against the count therefore stays open** | The real document is found and replaces the placeholder, or the 200-carton Dongle gap is explained (attrition, a second location, or a batch that did ship short) and recorded |
| 23 | `BLM18EG101TN1D` carries `Value = 68nH` where the ferrite-bead rule says impedance | conventions-library says a ferrite bead's `Value` is its impedance at the test frequency, and this part's `Impedance` property reads `100Ω@100MHz` while its `Value` reads `68nH`. Both are real published specs for the part, which is why it was left for a decision rather than changed — and why no check covers it: the Inductors `cmp.value_field` has no ferrite variant. Found in the 2026-07-25 Value audit and carried in the skill until the 2026-09-14 thinning moved it here | The user picks which spec the Value carries, the part matches it, and a ferrite variant of `cmp.value_field` enforces it |
| 24 | `Inductors`, `RF` and `Circuit_Protection` carry no `comp_type` | Their Value and description rules cannot become checks without one: a fixed inductor, a ferrite bead and a LAN transformer are three different rules sharing one category, with nothing in the data to tell them apart. Diodes shows the shape that works — `comp_type` of TVS / ZENER / DIODE, and `cmp.value_field` splits on it. `Connectors` has the same problem differently: `Pluggable terminal block; {Pitch}` and `Pluggable terminal block; 3.5mm` coexist in one family, so there is no single correct value to compare against | Each of the four categories carries a discriminator, and its Value rule is a variant group rather than a paragraph |
| 25 | Decide the symbol pin grid: 2.54 mm or 1.27 mm | `conventions-symbols` states 2.54 mm in both axes as absolute and claims an off-grid pin "cannot be wired on a default sheet". Measured 2026-09-14: `sym.pins_grid` enforces **1.27 mm** and no symbol violates it, while **61 of 206** are off 2.54 — `RPi_CM5` (200 pins), `ZED-F9P` (102), every large STM32. Dense parts do not fit on 2.54 mm, and KiCad's default schematic grid includes 50 mil, so the "cannot be wired" claim is not so either. The skill now records both numbers and says the choice is open | One number is the house rule, `sym.pins_grid` carries it, and the skill states it once |
| 26 | **`implied_devices` reads bare boards as assembled ones** | The importer takes a JLC order's device count from `pasteNumber`, and the comment at `services/jlc_import.py` ("Prefer JLC's own device count") states that field is what went through the assembly line, treating any gap below the billed `number` as free panel overage. On `SMT02404271716797` it is the opposite: 300 bare boards were ordered and 275 of them were assembled (user, 2026-09-18), so the platform credits CE_Dongle_V2 Batch 1 with 550 boards instead of 525. Every yield figure derived from it is then wrong — Batch 1 read as 29 boards short of its output when it is 4, which is ordinary test scrap and matches Batches 2 and 4. It is NOT the general case: Batch 3 is 44 short and Batch 5 is 32 short, and those boards were never programmed at all (see [reference/dongle-stock-reconciliation.md](reference/dongle-stock-reconciliation.md)). No other dongle order shows the bare-versus-assembled split, but no field in the payload distinguishes the two cases | The assembled quantity comes from a field that means assembled on every order, the Batch 1 figure reads 525, and the `pasteNumber` comment says what the field really carries |
| 27 | **Devices marked `in_stock` are online in the field** | The MQTT watcher ([0033](decisions/0033-the-broker-observes-devices-it-never-commands.md)) sees 37 of the 111 `in_stock` devices connected to the broker, and 17 of the 83 with no state. A device on the shelf has nothing to talk to, so either a `shipped` event was never recorded or the topic is live on hardware that is not the unit the row describes. It matters because `in_stock` is what fulfilment picks from, and [0032](decisions/0032-a-shipment-names-its-devices.md) makes a shipment name its devices — a shelf listing units already at a customer hands out serials nobody can deliver. Found 2026-09-18; not acted on, because the broker is evidence about a TOPIC and not proof about a row | A sample of the 54 is checked against the shelf, the cause is known, and the rows are corrected or the discrepancy is explained in a decision record |
| 22 | Three property families the conventions skill listed as follow-ups | Moved out of `conventions-library` on 2026-09-14, where a work register did not belong. (a) The ~36-member Connectors terminal-block family uses three free-text description conventions and wants one `{Pitch}`-based template; both Connectors chunks have to be reviewed together. (b) Buttons need verified Length/Width/Height/Force properties backfilled across the category before they can be templated. (c) BJTs carry `Continuous Drain Current` for collector current, a leftover from the MOSFET base template — the values are right, the key is wrong, and it moves as one pass across every sibling or not at all | Each family has one template in its category's `cmp.description` check, and the BJT key is renamed everywhere at once |
| 1 | Draw mechanical footprints for the three Italtronic enclosures — `Italtronic_05.0502530`, `Italtronic_35.0207000.BL`, `Italtronic_P05050201P.BL` | Every other `Mechanical_7S` enclosure has its own land (`Enclosure_Hammond_1551RFLGY`, `_1551TFLGY`, `_1551XFLGY`, `_1556CGY`, `Enclosure_TAKACHI_SIM6-12-3W`); these three are the only members of the family without one, so their outline and keepout are invisible on a layout. Raised by a review pass on 2026-08-25 and still flagged on all three parts. [Decision 0005](decisions/0005-off-board-parts.md) stopped the machine tier failing them but deliberately did not settle this — assigning a footprint puts each part back on the board automatically, so the two are compatible in either order | A footprint exists for each of the three, is assigned, and the `custom:footprint-missing-enclosure` flag on each part is answered rather than left open |
| 3 | Watch a manufacturer's product page, not only its PDF: register a page URL per component, re-open it on a schedule, and report new documents and lifecycle changes as findings | The original request behind [decision 0004](decisions/0004-datasheet-identity-and-storage.md), which built only the identity it needs. Measured 2026-09-10 over 20 vendor sites: a plain HTTP scrape finds the datasheet link on 3 of 17 pages, headless Chromium raises that to about 7, and Microchip, Analog Devices and ST refuse both from this network — so resolving a page must be a one-off step with a human or the agent confirming the document, while the nightly job stays a conditional GET on the resolved PDF. Chromium is in no image today. The orderable-part and lifecycle table the identity hash already extracts from a TI datasheet is the same signal, arriving for free | A component can carry a watched page URL, a scheduled pass reports a new document or a status change as a review finding rather than attaching it automatically, and a page the fetcher cannot reach is reported as unreachable instead of unchanged |

### Verification overhaul (agreed 2026-09-14)

Sequenced. Each phase is usable on its own, and each unblocks the next — the
order is not preference, it is dependency. **Rows 4, 5 and 6 are done**
(severity, standing exceptions, computed conformance) and were deleted from this
table as they landed; they live in decisions
[0016](decisions/0016-severity-and-standing-exceptions.md) and
[0017](decisions/0017-conformance-is-computed-not-recorded.md). Decisions
[0013](decisions/0013-the-validator-owns-the-automatic-checks.md),
[0014](decisions/0014-a-check-carries-its-own-configuration.md) and
[0015](decisions/0015-a-check-says-which-subjects-it-is-about.md) hold the
reasoning for what is already built; each phase below gets its own record
before it lands. **Every phase includes its UI — a feature the editor or the
review card cannot reach is not done.**

| # | Item | Why | Done when |
|---|---|---|---|
| 7 | Finish moving the convention skills into checks — `fp.pad_grid` | **The manufacturer `one_of` half is done**: it ships as `cmp.manufacturer_canonical`, a 99-name list at warning severity, carrying the names this row said were missing (`Nexperia`, `Littelfuse`, `Molex`, `Kingbright`, `Raspberry Pi`, `Quectel`) and resolving the `Murata` and `Infineon` duplicates. Two things remain on it: the list still holds BOTH `Kinghelm` and `Shenzhen Kinghelm Elec`, which is the duplicate shape the skill forbids, and four names (`TWGMC`, `Milliohm`, `TECH PUBLIC`, `Kinghelm`) need a maintainer decision. **`fp.pad_grid` does not exist.** Its fact `$footprint_off_grid_pads` is declared and unused. The blocker is NOT the provenance this row used to claim — `conventions-footprints` records it as measured 2026-09-14: the rule is about SPACING, not an absolute grid, because 76 of 150 coarse-pitch footprints have off-grid pads and are right to | The Kinghelm duplicate is resolved and the four open names decided; `fp.pad_grid` expresses the spacing rule and ships |
| 9 | A table-shaped rule, for the pin-type matrix | KiCad's ERC pin map is a 12x12 MATRIX, not a rule list — a reminder that not every rule fits the rule-list shape. `sym.pin_types` is still a plain judgement item with no structure (`services/checklists.py`). **The `disallow` half of this row is closed**: "there must be no X" ships as `at_most 0` and `absent`, both authorable in the checklist editor, and the sentence generator prints them forwards ("No plated holes whose copper is no wider than the drill") instead of backwards. It is live on `fp.zero_annulus`, `fp.model_path`, `cmp.pins_to_pads` and others. No assertion named `disallow` was added and none is needed | A decision record settles whether the pin-type rules want a table |
| 10 | **Claims with their own dependency, replacing the carry** | A record is versioned against the whole version, so a silkscreen edit can cost a pinout verification and 675 carry records exist to stop it. A claim should name what it depends on — the pin fingerprint, the datasheet identity, the property set — and be live while those hold. `services/material.py` already computes them, as one all-or-nothing gate | A claim carries `depends_on`; liveness is derived, not carried; `carry_geometry` and `carry_component` are gone |
| 11 | **Finish splitting the state into three facts** | The review card and the queue landed on 2026-09-14: a card reads `ISSUES · FAILS · JUDGED 10/11` and a queue row carries the same in its tooltip, because a DataTable row is one line tall and must not wrap. What is left is the surfaces that still read one word — the PROJECT REVIEW and the production RUN GATE, which decide whether a board may be built on a state that cannot tell "nobody looked" from "one item is open" | `conforms` / `judged n of m` / `signed` are separate on the project review and the run gate too |
| 14 | Retire `cmp.base_symbol_allowed` | Subsumed by `{fact: "$base_symbol", one_of: [...]}` once phase 8 lands. Two mechanisms for one rule is the thing decisions 0013-0015 exist to prevent | The key is out of `_CHECK_SPECS` and a migration has dropped the item from every stored list — a half-applied one leaves an item claiming `machine: true` for an unknown key, which the save path refuses |
| 16 | Narrow `custom:` keys once phase 7 lands | 188 distinct keys, 301 answers, 150 used exactly once, every one written by an agent. Those are 150 unfiled requests for a checklist item, recorded privately instead | An agent proposing a repeated custom key is steered to propose a checklist item; the one-off case still works |
| 17 | Decide `comp_type` versus subcategories | `comp_type` is a required property on Diodes, and is emitted to KiCad on 26 symbols in `Diodes.kicad_sym`. It is a subcategory wearing a property's clothes. **It is no longer unread**: `316cea0` added the `$symbol_comp_types` checklist fact, which the code calls "the bridge between the two axes", so a symbol rule can branch on the comp_type of the components drawn on it. Retiring the field now costs that mechanism too, which makes this MORE expensive to reverse than when the row was written, not less. `Category.parent_id` and the POST/PATCH/DELETE endpoints have always existed; the tree is flat and there is no screen. Costs ~41 published versions and their verifications | A decision record settles it; if subcategories win, the screen exists, the parts have moved, `comp_type` is retired and the variants keyed on it are deleted |
| 20 | Retire the checks the new ones supersede | The prefill landed on 2026-09-14 (CHANGELOG): eight checks live on production, measured, with the six documented Value deviations recorded as standing exceptions. What is left is the other half of the row — a check the new rules make redundant is still asked, so the library carries two rules for one thing. `cmp.base_symbol_allowed` is the known case (row 14); the rest wants one pass over the 25 component, 11 symbol and 20 footprint checks now that `assert` exists | Every check the new ones subsume is gone, and the removal is recorded |
| 21 | Repoint or retire the 188 agent-invented `custom:` keys | 301 answers, 150 keys used exactly once, every one written by an agent. Each is either a check that should exist (repoint it) or a genuine one-off. Left alone they are 188 private vocabularies competing with the real checklist | Every custom key is either mapped onto a real check or deliberately kept; the mapping is recorded; `custom:` is narrowed per row 16 |

## More information

* [decisions/index.md](decisions/index.md) — decisions still `proposed`.
* [CLAUDE.md](../CLAUDE.md) — how a finding routes to a decision, a skill, a
  checklist item or this file.
